# Write assignment in GitHub
# Write Submission from GitHUB to transfer to Google Cloud

# Access file student submission
access csv file at 
import file from https://console.cloud.google.com/storage/browser/_details/bucket-quickstart_pcc-grading-project-demo/Student%20Completed%20Assignment.csv;tab=live_object?project=pcc-grading-project-demo

import urllib.request

with urllib.request.urlopen('<some url>/') as response:

html = response.read()

urllib.request.urlopen(https://console.cloud.google.com/storage/browser/_details/bucket-quickstart_pcc-grading-project-demo/Student%20Completed%20Assignment.csv;tab=live_object?project=pcc-grading-project-demo)

import urllib2

data = urllib2.urlopen("https://console.cloud.google.com/storage/browser/_details/bucket-quickstart_pcc-grading-project-demo/Student%20Completed%20Assignment.csv;tab=live_object?project=pcc-grading-project-demo").read(20000) # read only 50 000 chars
data = data.split("\n") # then split it into lines

for line in data:
    print line

create a table name and phone number


Send table to google cloud student submission with apend list
Function # Validate phone number
run loop phone comparison of string
compare names to see if two are the same  
  else if phone number compare if same ignore, if different add+-A to the end of duplicate in table
run loop to compare if 2 numbers are the same  if they are the same both get a score of -1 with Feedback "Pagerism Detected"

run loop on proper format if format is correct and no plagerism is detected scote at 1 with feedback "GoodWork"
elif no plagerism is detected and format is false then score is 0 with feedback "incorrect Format"
Access file student submission
review file 



# Validate phone number
import re

validate_phone_number_pattern = "^\\+?[1-9][0-9]{7,14}$"
re.match(validate_phone_number_pattern, "+12223334444") # Returns Match object

# Extract phone number from a string
extract_phone_number_pattern = "\\[1-9]"-"[0-9]"-"{7,14}"
re.findall(extract_phone_number_pattern, 'You can reach me out at 222-333-4444 and 666-777-8888') # returns ['222-333-4444', '666-777-8888']

os.mkdir()

# Python program to explain os.mkdir() method

# importing os module
import os

# Directory
directory = "GeeksforGeeks"

# Parent Directory path
parent_dir = "D:/Pycharm projects/"

# Path
path = os.path.join(parent_dir, directory)

# Create the directory
# 'GeeksForGeeks' in
# '/home / User / Documents'
os.mkdir(path)
print("Directory '% s' created" % directory)

# Directory
directory = "Geeks"

# Parent Directory path
parent_dir = "D:/Pycharm projects"

# mode
mode = 0o666

# Path
path = os.path.join(parent_dir, directory)

# Create the directory
# 'GeeksForGeeks' in
# '/home / User / Documents'
# with mode 0o666
os.mkdir(path, mode)
print("Directory '% s' created" % directory)


